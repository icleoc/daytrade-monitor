services:
  - type: web
    name: daytrade-monitor
    env: python
    pythonVersion: "3.12.17"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python run_server.py"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: "10000"
